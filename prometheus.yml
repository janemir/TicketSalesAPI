global:
  scrape_interval: 5s 

scrape_configs:
  - job_name: 'service-db' 
    static_configs:
      - targets: ['service-db:8080'] 

  - job_name: 'service-nodb'
    static_configs:
      - targets: ['service-nodb:8080']